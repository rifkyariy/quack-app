
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# macOS
.DS_Store

# Xcode per-user state
xcuserdata/
*.xcuserstate

# Local tooling — agent/issue-tracker state, not for the remote
.beads/
.claude/
.superpowers/

# Agent instructions — local only, kept out of the remote
CLAUDE.md
AGENTS.md
